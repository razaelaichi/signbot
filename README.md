# signbot
A single-neuron neural network trained on 100 numbers to classify whether an input is positive or negative, along with prediction confidence. Built to understand how neural networks learn from data instead of using hardcoded logic. Made completely from scratch without AI-generated code.
